import React from 'react';

const HeroBanner = () => {
    return (
        <section className={`bg-[url('/src/assets/hero-banner.png')] w-full bg-no-repeat bg-cover h-screen`}>

        </section>
    );
};

export default HeroBanner;